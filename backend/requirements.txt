# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data validation
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.1.1

# Database
sqlalchemy>=2.0.30,<2.1.0
asyncpg==0.29.0
alembic>=1.13.1,<2.0.0

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# File uploads
python-multipart==0.0.9

# HTTP client (calls to Whisper + Ollama)
httpx==0.27.0

# Object storage (MinIO / S3-compatible)
aioboto3==13.1.1
boto3==1.34.131

# Background jobs
arq==0.25.0
redis[hiredis]==5.0.4

# PDF text extraction
pypdf==4.2.0

# TTS for AI reference reading — Indian English via Google (co.in accent)
gTTS==2.5.3

# Linting / formatting (dev)
ruff==0.4.7

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
